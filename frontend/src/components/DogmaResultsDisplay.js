/**
 * DogmaResultsDisplay - DISABLED component
 * Dogma results now show in ActionLog only for better UX
 */
import React from "react";

function DogmaResultsDisplay() {
  // DISABLED: DogmaResultsDisplay is permanently disabled
  // Dogma results now show in ActionLog only for better UX
  return null;
}

export default React.memo(DogmaResultsDisplay);
